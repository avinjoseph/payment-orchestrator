# pytest.ini
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    unit: Fast unit tests in isolation
    scenario: End-to-end failure mode and reliability scenarios